# Listflow

AI-powered eBay listing assistant for resellers. Snap photos of items → AI identifies the item, suggests a title and pricing → review and post to eBay.

## Tech Stack

| Layer | Tech |
|---|---|
| Framework | Next.js 16.3.4 (App Router, TypeScript, React 19) |
| Styling | Tailwind CSS |
| Icons | Lucide React |
| AI | OpenAI GPT-4o-mini (vision) |
| Database | Supabase (PostgreSQL) |
| Deployment | Vercel |

Environment variables required:
- `OPENAI_API_KEY` — server-side only
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`
- `EBAY_CLIENT_ID`, `EBAY_CLIENT_SECRET`, `EBAY_RUNAME` — the one registered eBay application, correctly shared across all users (each user still does their own OAuth consent against this one app — see the Phase 2 note below).
- `EBAY_TOKEN_ENCRYPTION_KEY` — 32-byte, base64-encoded AES-256-GCM key used to encrypt each user's eBay refresh token before it's stored in `ebay_connections`. Generate with `node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"` and paste directly into Vercel — never share it elsewhere.
- `SUPABASE_SERVICE_ROLE_KEY` — server-side only, never sent to the browser. Used by `/api/auth/lockout` (a thin proxy for the three login-lockout RPCs — see the Security hardening note below) to call Postgres functions that are no longer grantable to `anon`/`authenticated`. Get it from Supabase dashboard → Settings → API → service_role key, and paste directly into Vercel — never share it elsewhere; it bypasses RLS entirely.
- `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN` — optional. Powers rate limiting across the API surface (see below). If unset, rate limiting is silently skipped rather than the app failing.
- `NEXT_PUBLIC_SENTRY_DSN` — optional but set. Sentry error monitoring (see below). Not a secret — Sentry DSNs are designed to be public (they can only submit events to this project, never read anything back) and this one is intentionally a `NEXT_PUBLIC_` var so the browser-side config can use it too.
- `CRON_SECRET` — required for the account-deletion purge job (`GET /api/cron/purge-deleted-accounts`) to accept requests. Vercel automatically sends `Authorization: Bearer $CRON_SECRET` on its own Cron-triggered requests once this env var is set — generate any random string (e.g. `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`) and paste it into Vercel; it isn't used anywhere else.

**Deprecated as of Phase 2 (per-user eBay connections) — no longer read by the app, safe to remove from Vercel whenever convenient:** `EBAY_OAUTH_REFRESH_TOKEN`, `EBAY_SHIPPING_FREE_ID`, `EBAY_SHIPPING_HEAVY_ID`, `EBAY_SHIPPING_CALCULATED_ID`, `EBAY_RETURN_POLICY_ID`. These used to be one shared seller connection/policy set for the whole app; each user now connects their own eBay account and picks their own policies in Settings → eBay Connection.

## What's Built

### Pages
- `/` — Landing/splash page
- `/login` — Supabase sign-in/sign-up
- `/forgot-password`, `/reset-password` — self-service password reset, added Sept 9, 2026 (see the dated bullet below). Both public routes, added to `PUBLIC_PATHS` in `src/middleware.ts`.
- `/dashboard` — Live stats (drafts, active listings, weekly revenue + sold count) + quick action cards. "To ship" card shows urgent badge when items are awaiting shipment.
- `/new-listing` — Single item flow: 3-photo slots (front, measurements, flaws) → AI analysis + live pricing in parallel → save draft / list on eBay
- `/batch-upload` — Multi-item workflow: upload → AI photo grouping → manual review → bulk analysis → results with bulk list/save actions. Grouping has two free/deterministic alternatives to the default AI-similarity grouping — see the item-dividers bullet below.
- `/drafts` — Unlisted drafts with thumbnails, pricing info, no-price warning, bulk delete, bulk list with price guard
- `/drafts/[id]` — Edit draft, AI suggest specifics, re-analyze with new photos, list / relist on eBay
- `/store` — Active + recently-ended eBay listings (Supabase + eBay merged), tabbed (Active / Ended). Active tab: search, sort, inline price edit, bulk price update, delist. Ended tab: view-only listing details plus a real "Relist" action (Trading API `RelistFixedPriceItem`, not a link out to eBay) that brings it back as a new active listing. Also links to `/offers` (Pending offers). eBay only retains a recent window of ended-unsold listings — there's no way to widen that from our side.
- `/sales` — Sales history with 7d/30d/90d/1y toggle, total revenue, per-item thumbnails
- `/ship` — Items paid but not yet shipped: buyer name/address, days-since-payment badge, Ship → link to eBay order
- `/membership` — Pricing plans UI only (Stripe deferred)
- `/privacy`, `/terms` — real Privacy Policy / Terms of Service content (rewritten Sept 3, 2026 from a four-paragraph placeholder — see the Legal content bullet below), public (no login required). Linked from the landing page footer, a consent line on `/login`, and a Legal footer on `/settings` — none of which existed before.
- `/settings` — App-wide defaults, redesigned Sept 2, 2026 into distinct cards (each with an icon badge header, via `SettingsSection` in `src/app/settings/page.tsx`) instead of plain stacked text labels: **Default shipping** (Free / Calculated, every new listing starts on this), **Batch upload: item dividers** (opt-in AI-detected number-marker grouping — see the item-dividers bullet below), **eBay Connection** (connect/reconnect, Business Policy pickers), **Appearance** (Light / Dark / System — see Theming below), **Account** (sign out; Danger zone → Delete account, see the account-deletion note below). Linked from a gear icon on `/dashboard`.
- `/account/pending-deletion` — the only page a signed-in user with a pending deletion request can reach; shows a day countdown and a one-click Reactivate (see below).
- `/offers` — pending Best Offers (shipped earlier Sept 9, 2026 — see below); linked from a "Pending offers" card on both `/store` and `/dashboard` now.
- `/messages` — unanswered buyer questions (Trading API `GetMemberMessages`/`AddMemberMessageRTQ`), added Sept 9, 2026 — see below; linked from a "Buyer questions" card on both `/store` and `/dashboard`.

### API Routes
- `POST /api/analyze-item` — GPT-4o-mini vision, 1–3 photos; returns itemType, brand, color, size, condition, flaws, title, style, material, pattern, fit, vintage, theme, character, yearManufactured, season, description, and measurements (pitToPit, length, waist, inseam read from measuring tape in photo)
- `POST /api/analyze-batch` — Sequential batch analysis with rate limit handling and retry logic
- `POST /api/group-photos` — GPT-4o-mini clusters mixed uploaded photos into per-item groups by visual similarity
- `POST /api/detect-item-dividers` — opt-in alternative to the above (Settings → "Batch upload: item dividers" → On): GPT-4o-mini classifies each photo as a numbered marker or not, instead of comparing photos for similarity — see the item-dividers bullet below
- `GET|POST|DELETE /api/drafts` — Supabase CRUD for saved drafts
- `GET /api/drafts/[id]` / `PATCH /api/drafts/[id]` / `DELETE /api/drafts/[id]`
- `POST /api/drafts/[id]/duplicate` — copies a draft's descriptive/eBay-facing fields into a brand new draft; clears `photo_urls`/`thumbnail_url`/`custom_sku`/`ebay_listing_id` since those must be specific to the physical item, not the description — added Sept 10, 2026, see below
- `POST /api/ai/suggest-specifics` — fills eBay item specifics from existing draft fields
- `POST /api/pricing/suggest` — live pricing via eBay Browse API (image search → text fallback → condition-adjusted median)
- `GET /api/ebay/ship` — paid-but-unshipped orders from GetSellerTransactions
- `GET /api/ebay/sales?days=7|30|90|365` — sales history (multi-window, chunked into 30-day GetSellerTransactions calls and merged; capped at 365 days)
- `GET /api/ebay/store` — active + ended-unsold listings via GetMyeBaySelling (ActiveList + UnsoldList, each independently paginated)
- `GET /api/ebay/inventory` — Supabase-sourced listings (instant load for store page)
- `POST /api/ebay/list` — full listing flow: upsert inventory → create/update offer → publish
- `POST /api/ebay/delist` — end listing, clear from Supabase
- `POST /api/ebay/update-price` — ReviseFixedPriceItem + update Supabase
- `GET /api/ebay/connect` / `GET /api/ebay/callback` — OAuth flow
- `GET /api/dashboard/stats` — aggregated stats for dashboard
- `POST /api/auth/lockout` — server-side proxy for the three login-lockout Postgres RPCs (`check`/`record`/`clear` actions), called from `/login` before/after `signInWithPassword`. Exists so those RPCs can be locked down to `service_role` only instead of staying callable by anyone via Supabase's public REST RPC endpoint — see the Security hardening note below.
- `GET|PATCH /api/settings` — reads/writes the per-user `app_settings` row (`default_shipping_mode`, `deletion_requested_at`)
- `GET /api/ai-usage` — read-only lookup of the caller's own current-month AI usage (`{used, limit}`), backing the 75%-of-cap nav banner — see the bullet below
- `POST /api/account/delete` — starts the 30-day account-deletion grace period; deletes nothing itself
- `POST /api/account/reactivate` — cancels a pending deletion request
- `GET /api/account/status` — pending state + days remaining, used by Settings and `/account/pending-deletion`
- `GET /api/cron/purge-deleted-accounts` — Vercel Cron only (`CRON_SECRET`-gated); permanently deletes any account whose grace period has elapsed
- `GET /api/ebay/store-categories` — fetches the seller's eBay Store custom categories (Trading API `GetStore`), flattened to leaf categories with a "Parent / Child" display path; returns `{categories: [], connect: true}` if eBay isn't connected
- `GET /api/ebay/offers` — pending Best Offers across active listings (Trading API `GetBestOffers`, bounded worker-pool, capped/truncated) — shipped Sept 9, 2026, missing from this table until now
- `POST /api/ebay/offers/respond` — accept/decline/counter a pending Best Offer (`RespondToBestOffer`) — shipped Sept 9, 2026, missing from this table until now
- `POST /api/ebay/relist` — brings an ended, unsold listing back as a new active one (Trading API `RelistFixedPriceItem`)
- `GET /api/ebay/messages` — unanswered buyer questions across active listings (Trading API `GetMemberMessages`, single call, title/thumbnail joined from `ActiveList`) — added Sept 9, 2026
- `POST /api/ebay/messages/respond` — reply to a buyer question (`AddMemberMessageRTQ`) — added Sept 9, 2026

### Notable Implementation Details
- **node:https for all eBay + OpenAI calls** — Next.js 14 patches `globalThis.fetch` which breaks repeated outbound HTTPS. All external API calls use `node:https` directly. Every route using it must export `runtime = "nodejs"`.
- **eBay Trading API URL**: `https://api.ebay.com/ws/api.dll` (not `/ws/services`)
- Client-side image resizing before upload (max 1568px) to reduce API token cost
- Batch upload uses explicit step state machine: `upload → grouping → review → analyzing → results`
- **Batch processing is bounded-concurrency, not fully sequential.** `/batch-upload`'s AI analysis pool (`ANALYSIS_CONCURRENCY` in `src/app/batch-upload/page.tsx`) runs 3 items at once — the same worker-pool pattern already used for its pricing lookups (`PRICING_CONCURRENCY`) — instead of one at a time; bulk "List on eBay" (`LISTING_CONCURRENCY`) runs 2 at once, kept lower since a single listing is already several sequential eBay calls internally. `/api/analyze-batch` itself still retries each item up to 3 times with a 15s backoff on an actual OpenAI rate-limit response — that safety net is unchanged, it just now backs up 3 lanes instead of 1. The real ceiling on how much this can help is OpenAI's own per-minute token budget on the account (visible in a 429's error text), not something raising these numbers further can fix on its own.
- AI measurements injected as first line of `description` field — not stored as separate DB columns
- GetSellerTransactions requires `<DetailLevel>ReturnAll</DetailLevel>`, but — despite what an earlier version of this doc claimed — its `<Item>` block never includes `PictureDetails`/`GalleryURL` no matter the DetailLevel (confirmed against eBay's own docs, which list GetSellerTransactions' limited Item fields explicitly and point to `GetItem` for anything more). `/api/ebay/sales` gets sold-item thumbnails from two places instead: the Supabase `drafts.thumbnail_url` for anything listed through this app (instant, no extra call), and a `GetItem` call per remaining ItemID (concurrency-limited to 5, capped at 60 lookups per page load) for older/manually-listed items with no Supabase row. Note eBay stops returning item details (pictures included) for listings that ended more than ~90 days ago, so some very old sales can still show the placeholder icon — a real eBay limitation, not a bug.
- GetSellerTransactions ModTime cap is 30 days — anything beyond that (up to the 365d cap) uses parallel window calls merged with dedup. **Fixed Sept 3, 2026:** those merged results were never sorted, so /sales rendered sales in whatever order the parallel per-window calls happened to resolve in (visibly out of date order once more than one window was involved) instead of most-recently-sold first. `/api/ebay/sales/route.ts` now explicitly sorts by `soldAt` descending after the merge.
- GetMyeBaySelling's `ActiveList` only ever returns *currently active* listings — it never included ended/unsold ones. `/store` now also requests `UnsoldList` (same pagination pattern, independent totals) so "ended without selling" listings are visible too, not just active ones. Sold listings still live only under `/sales` (GetSellerTransactions), not here.
- **Shipping mode, fixed Sept 10, 2026.** `shippingMode: "free" | "buyer_pays" | "calculated"` (`src/lib/shipping.ts`; parsed from request bodies with `parseShippingMode`) is now persisted per draft (`drafts.shipping_mode`, migration `020_draft_shipping_mode.sql`) and selectable on every listing screen. New Listing, Batch Upload, and Draft Details each show a per-item dropdown for Free, Calculated, or the legacy flat-rate buyer-pays option; bulk listing reads the saved draft mode instead of relying on the old Heavy item checkbox/localStorage fallback. If a request omits the mode, `/api/ebay/list` still honors the saved draft mode, then the seller's Settings default, then the legacy Heavy item flag. Pricing only folds estimated shipping into the item price for Free mode, while Calculated mode attaches the estimated package weight/dimensions so eBay can quote the buyer's location-specific rate. This threads into pricing (`computeListAndFloor` in `src/lib/pricing.ts`) and into the actual eBay setup applied:
  - **Free**: seller's own "free shipping" policy (picked in Settings → eBay Connection, stored in `ebay_connections.shipping_free_policy_id`); full estimated shipping cost baked into the item price before grossing up for eBay fees.
  - **Buyer pays (flat)**: seller's own flat-rate "buyer pays" policy (`shipping_heavy_policy_id`) with a `shippingCostOverrides` set to the seller-chosen dollar amount; item price only needs to recover the *fee portion* of that shipping charge (buyer fronts the cost itself) — real margin the "free" math would otherwise absorb.
  - **Calculated**: seller's own "cost varies by buyer location" policy (`shipping_calculated_policy_id`) plus a `packageWeightAndSize` (weight + box dimensions + `packageType`) attached to the inventory item itself (`upsertInventoryItem` in `src/lib/ebay-inventory.ts`) so eBay can quote each buyer their own real carrier rate at checkout. No dollar override is sent (or possible) for this mode — same fee-only pricing treatment as buyer-pays, using the app's weight estimate as the best available proxy since the real per-buyer charge isn't known ahead of time.
  All three policy IDs (plus the return policy) come from `getEbayContext().policies` (`src/lib/ebay-request-context.ts`), populated per-request from that user's `ebay_connections` row — see the Phase 2 note below. Before Phase 2, these were one shared set of env vars.
  Both the cost estimate and the calculated-mode package weight/dimensions come from `src/lib/shipping.ts`'s weight-based estimate (`estimateShipping`/`estimatePackage`, from AI-detected item type/size/material against USPS Ground Advantage weight breaks and reasonable per-weight-bracket box sizes) — an estimate, not a measurement, same tradeoff as before.
- All eBay-dependent pages return `connect`/`reconnect` flags for missing/expired token UI
- **eBay Store Categories: the backend was real but the UI was never built — found Sept 3, 2026, fixed Sept 9, 2026.** This doc used to describe a `StoreCategoryControl` dropdown with an AI-suggested pre-selection that never actually existed: a repo-wide search turned up zero `.tsx` references to `store-categories`/`storeCategoryId` anywhere. What was already real and unchanged by this fix: `GET /api/ebay/store-categories` (fetches + flattens the seller's real Store categories, 30-minute in-memory cache), the `drafts.store_category_id`/`store_category_name` columns, and the best-effort `setListingStoreCategory()` Trading API call in `/api/ebay/list/route.ts` that applies a category to a freshly-published listing once `storeCategoryId` is set. That last part was the gap — no code path ever set it. See the "Store category suggestions" bullet below for what was built to close it.
- **Bulk-edit on batch-upload results, shipped Sept 3, 2026.** The results screen (after AI analysis, before saving) had no way to change condition or heavy-item shipping across more than one item at a time — a seller running 40 similar items through in one sitting had to open and edit each row individually. Added a selection bar above the results list (`src/app/batch-upload/page.tsx`): a checkbox on each not-yet-saved item plus "select all," and once anything is selected, inline controls to set condition or heavy-item + shipping cost across every selected item at once. Deliberately scoped to items that haven't been saved yet (`!draftIds[i]`) — every per-item field already locks with `disabled={!!draftIds[i]}` once a draft is saved, so bulk-applying to an already-saved item would silently do nothing. Store category joined this bulk-edit too, Sept 9, 2026 -- see the "Bulk-apply store category" bullet below.
- **Rate limiting, two tiers, across the whole API surface** (`src/middleware.ts`, Upstash Redis via `@upstash/ratelimit`/`@upstash/redis`) — not just the two AI vision endpoints anymore. Tier 1 (15 req/min/IP): every endpoint that triggers a real paid OpenAI call or fans out several eBay calls per invocation — `/api/analyze-item`, `/api/analyze-batch`, `/api/group-photos`, `/api/ai/suggest-specifics`, `/api/pricing/suggest`. Tier 2 (60 req/min/IP): every other `/api/*` route (excluding the eBay OAuth callback), a catch-all that used to have zero rate limiting at all. Built defensively: if `UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN` aren't set, rate limiting is silently skipped rather than crashing every request through middleware — a missing secondary feature should never take down the whole app.
- **Per-user data isolation (Phase 1 of the multi-tenant plan, shipped).** `supabase-migrations/003_multi_tenant_isolation.sql` converted `drafts.user_id` from dead `text default 'default-user'` to a real `uuid` FK against `auth.users(id)`, rewrote RLS on `drafts`/`app_settings`/the `photos` storage bucket to be scoped to `auth.uid()` instead of just "authenticated," and converted `app_settings` from one shared singleton row to one row per user (auto-created on first Settings save via upsert in `/api/settings`). Photo uploads are namespaced by user folder (`src/lib/storage.ts`) to match.
- **Per-user eBay connections (Phase 2 of the multi-tenant plan, shipped).** `supabase-migrations/004_ebay_per_user_connections.sql` adds `ebay_connections` (one row per user: encrypted refresh token, own Business Policy IDs). Each eBay-touching API route calls `requireEbayConnection()` (`src/lib/ebay-connection.ts`) then runs its logic inside `ebayContext.run(...)` (`src/lib/ebay-request-context.ts`, a Node `AsyncLocalStorage`) instead of every function in `ebay-inventory.ts`/`ebay-store-categories.ts` taking a userId parameter — `getAccessToken()` and the Store Category cache read the connection from that implicit per-request context, keyed by userId internally so two users' requests landing on the same warm serverless instance can never cross-contaminate tokens/categories. The refresh token is encrypted at rest (`src/lib/ebay-token-crypto.ts`, AES-256-GCM via `node:crypto`, key in `EBAY_TOKEN_ENCRYPTION_KEY`) — `/api/ebay/callback` stores it there directly now instead of displaying it for manual copy-paste into an env var. Business Policies (shipping/return) are picked in Settings → eBay Connection from a live list fetched via eBay's Account API (`/api/ebay/policies`, `sell.account.readonly` scope), same "seller picks from what's actually theirs" pattern as Store Categories. **The one eBay app registration itself** (`EBAY_CLIENT_ID`/`SECRET`/`RUNAME`) stays a shared env var — that's normal, it's the app every user OAuths into, not a seller-specific credential. Anyone connected before this scope was added needs to reconnect once (Settings → Reconnect) to pick it up.
- **Security hardening pass (Sept 2026).** Four fixes from a full code + Supabase-advisor audit: (1) HTTP security headers (CSP, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, HSTS) added via `headers()` in `next.config.js` — previously there were none at all; (2) rate limiting extended from 2 routes to the whole API surface, see the bullet above; (3) `store_category_id` is now validated as a numeric string at both API boundaries that accept it (`POST /api/drafts`, `PATCH /api/drafts/[id]`) — it was always eBay-numeric and always dropdown-picked, never hand-typed, so a non-numeric value only ever meant a bug or a tampered request; `/api/drafts/[id]`'s GET/PATCH/DELETE also gained an explicit `.eq("user_id", ...)` check alongside RLS (belt-and-suspenders against a cross-account ID guess), same for the bulk delete in `/api/drafts`; (4) the three login-lockout Postgres functions (`check_login_lockout`/`record_failed_login`/`clear_login_attempts` — see the `login_attempts` table note below) were revoked from `anon`/`authenticated` and re-granted to `service_role` only (`supabase-migrations/006_lock_down_login_rpcs.sql`) — they used to be callable by anyone directly against Supabase's public REST RPC endpoint with zero rate limiting, including to lock a real user out by spamming `record_failed_login` for their email. `/login` now calls a new `POST /api/auth/lockout` route instead, which proxies those three RPCs with a `SUPABASE_SERVICE_ROLE_KEY` client — so every call is now subject to the app's own rate limiting like everything else.
- **No self-service sign-up, still on purpose (for now).** `/login` only offers sign-in, never account creation — see the comment at the top of `src/app/login/page.tsx`. The two legitimate accounts were created directly in the Supabase dashboard. **The UI change alone is not sufficient** — "Allow new users to sign up" should also be disabled in the Supabase dashboard (Authentication → Sign In / Providers → Email), since that's enforced by Supabase itself and can't be bypassed by calling its REST API directly with the public anon key the way an app-level check could be. Sign-up stays closed for now regardless — the owner is running a personal test period before deciding to open it, independent of whether the technical guardrails below are in place.
- **Phase 3 spending safety net, shipped Sept 3, 2026 — a hard per-account monthly AI usage cap.** `supabase-migrations/010_ai_usage_cap.sql` adds `ai_usage_counters` (one row per user: `period_month`, `call_count`) plus a SECURITY DEFINER function `check_and_consume_ai_usage(user_id, amount, monthly_limit)` that atomically checks-and-increments, resets automatically when the calendar month rolls over, and is locked down to `service_role` only — same pattern as the login-lockout RPCs (005/006/007), unreachable from the browser or Supabase's public REST RPC endpoint. `src/lib/ai-usage.ts` wraps it (`checkAndConsumeAiUsage`, `MONTHLY_AI_CALL_LIMIT = 400/month`, tunable without a migration) and fails open on any Supabase error, matching the fail-open choice already made for Upstash rate limiting when its env vars are unset. Wired into all four OpenAI-calling routes: `/api/analyze-item`, `/api/group-photos`, and `/api/ai/suggest-specifics` each consume 1 call and return a 429 with a plain-English message once the cap is hit; `/api/analyze-batch` consumes one call per group in the batch (not one per request) and, if a batch doesn't fully fit the month's remaining budget, processes as many groups as still fit and marks the rest capped in the results array — rather than failing the whole batch over a partial shortfall. This removes the last technical blocker to opening sign-up (see the bullet above) — the decision to actually open it is still the owner's, separate from whether it's safe to.
- **Self-service account deletion, with a 30-day grace/reactivation period (shipped Sept 2, 2026, once Phase 1's per-user data isolation made it safe).** An earlier `DELETE /api/user/delete` route was deliberately left unmerged because, pre-Phase-1, `drafts` had no per-user ownership column — deleting "your own" data would have deleted everyone's. That's fixed now (see the Phase 1 bullet above), so this shipped as a real soft-delete flow rather than an instant, irreversible one: Settings → Account → Danger zone → Delete account (`POST /api/account/delete`) just timestamps `app_settings.deletion_requested_at` and signs the user out — nothing is actually deleted yet. `src/middleware.ts` then locks any signed-in user with that flag set to `/account/pending-deletion` (and the couple of API routes that page itself calls) until they either hit "Reactivate" there (`POST /api/account/reactivate`, clears the flag) or the grace period (`ACCOUNT_DELETION_GRACE_PERIOD_DAYS = 30`, `src/lib/account-deletion.ts`) elapses. A daily Vercel Cron job (`vercel.json`, hits `GET /api/cron/purge-deleted-accounts`, gated on the `Authorization: Bearer $CRON_SECRET` header Vercel sends automatically) finds accounts past that window, deletes their `photos` storage files by hand first (storage objects aren't FK'd to `auth.users`, so it's the one thing that isn't automatic), then calls `supabase.auth.admin.deleteUser()` — which cascades to `drafts`, `ebay_connections`, and `app_settings` for that user alone via the `on delete cascade` FKs from migration 003. One account's cleanup failing doesn't block the rest of that day's batch; it's simply retried on the next run since `deletion_requested_at` stays set until the delete actually succeeds. Migration: `supabase-migrations/008_account_deletion.sql`.
- **Real Terms/Privacy content, drafted Sept 3, 2026 from comparative legal research (not attorney-written).** `/terms` and `/privacy` used to be four generic paragraphs each with no links anywhere in the app pointing to them. Now cover: the AI-suggestion accuracy disclaimer, AS-IS/liability-cap and indemnification clauses, prohibited use, account termination, Florida governing law (Miami-Dade County venue — Sel is FL-based, sole proprietor, no LLC yet), named third-party data sharing (Supabase/OpenAI/eBay/Vercel), the account-deletion grace period (see above), children's privacy, and user rights. Contact email on both pages is a Gmail "+" alias (`funkyvaultvintage+listflow@gmail.com`), not the bare account email. Full sourcing (eBay/Poshmark/Vinted/Vendoo/Crosslist comparisons, FDUTPA, Florida's marketplace-facilitator statute, the Olmstead single-member-LLC gap, etc.) lives in the "Listflow Legal Ledger" reference artifact, not in the repo — this is a starting point for attorney review, not final legal text.
- **AIDisclaimer placement bug, fixed Sept 3, 2026.** `src/components/AIDisclaimer.tsx` ("AI suggestions may be imperfect. Review the title, condition, price, and item details before listing.") already existed and was used on all three AI-suggestion screens (`new-listing`, `batch-upload`, `drafts/[id]`) — but on the latter two it was wrapped in `{error && (...)}`, so it only rendered when something had already gone wrong, never during normal use. Moved to render unconditionally right where the AI-suggested title/price actually appear (top of the `results` step in batch-upload; right above the editable Title field in drafts/[id]) — `new-listing` already had this right and was left alone.
- **RLS query-plan performance fix, Sept 3, 2026.** A Supabase advisor scan (part of the same public-launch-readiness audit as the AI usage cap above) flagged `auth_rls_initplan` on every policy in `drafts`, `app_settings`, and `ebay_connections`: each called `auth.uid()` directly in its `USING`/`WITH CHECK` clause, which Postgres re-evaluates once per row instead of once per query — invisible with a handful of rows, a real cost once an account's row counts grow. `supabase-migrations/009_rls_performance.sql` rewrites all eleven policies to `(select auth.uid())`, which Postgres caches for the whole query. No access-rule change, confirmed clean on a re-run of `get_advisors`.
- **Theming (light/dark/system)** — CSS custom properties (`--bg-page`, `--bg-surface`, `--text-primary`, `--danger`, `--warning-bg`, etc. — see `src/app/globals.css`) are defined three times: once on bare `:root` (light, the default), once under `@media (prefers-color-scheme: dark)` scoped to `:root:not([data-theme="light"])` (follows the OS/browser setting when the seller hasn't picked one explicitly), and once under `:root[data-theme="dark"]` (an explicit choice always wins, either direction). The choice is stored in a plain `theme` cookie (`src/lib/theme.ts`) — **not** localStorage — specifically so `RootLayout` (`src/app/layout.tsx`, a server component) can read it with `cookies()` and render the correct `data-theme` attribute into the very first byte of HTML. That avoids the "flash of wrong theme" that localStorage-based approaches need a blocking inline `<script>` in `<head>` to paper over. `.card`/`.btn`/`.btn-primary`/`.input` (used as plain classNames across ~20 files) live in the same file and are what actually give cards/buttons/inputs their background, border, and shadow — they read from the same variables, so they theme automatically too.
  **Regression history, fixed Sept 2, 2026:** an earlier session (limited by a stale device connection) rebuilt `globals.css` from a `next build`-verification stub and never restored the real file — the rebuilt version kept only `--brand-500/600`, `--bg-page`, `--text-primary/secondary/tertiary`, and `--border`, silently dropping `--bg-surface`, `--danger`, `--success`, `--warning-bg`, `--warning-border`, **all** dark-mode values, and the `.card`/`.btn`/`.input` class definitions themselves. Net effect: every card/button/input in the app rendered with zero styling from those classes (whatever shape they had came only from other literal Tailwind utilities on the same element), and Settings → Appearance's Dark/System options changed the `data-theme` attribute correctly but nothing was listening for it, so the toggle visibly did nothing. Confirmed via `git log -p -- src/app/globals.css` (the stub's own commit message admits it wasn't the real file) and via grepping compiled output (`.next/static/css/*.css` had zero occurrences of `.card` before the fix, all expected rules present after). Restored the full variable set + component classes + dark-mode blocks; if a future device-access gap ever forces a placeholder file again, treat that placeholder as temporary and diff it back out before considering the fix done — don't let a stub become the permanent file a second time.

- **Performance audit, Sept 3, 2026 — eBay listing/delisting sped up, no behavior change.** A full read-through of every API route and lib for unnecessary serialization found two real wins on the app's most-used actions (List on eBay, Delist), both fixed:
  1. **SKU cleanup loops parallelized.** `/api/ebay/list` and `/api/ebay/delist` each clean up stale offers/inventory items across up to 5 legacy SKU formats before (re)listing — this used to run one full SKU at a time (find its offers → delete each → delete the inventory item, then repeat for the next SKU), even though every SKU's cleanup is completely independent of every other's. Both routes now fire all candidate SKUs' cleanup concurrently with `Promise.all` (offers within a SKU also delete concurrently). Cuts several sequential eBay round trips down to the slowest single one on every listing/delisting action.
  2. **`ensureMerchantLocation()` cached per user.** This is a one-time, idempotent setup call (a repeat POST just gets a 409 "already exists" back, already handled as success) but was called unconditionally on every single "List on eBay" request — pure wasted latency after the first successful call for a given seller. Now cached in an in-memory `Set<userId>` (`src/lib/ebay-inventory.ts`) and skipped once a user's location is confirmed set up; cleared by `recreateMerchantLocation()` so a real teardown still gets re-verified.
  Parallelizing the SKU loop meant several `getAccessToken()` calls for the same user could now land on a cold token cache at the same instant — without a fix, each would fire its own redundant refresh request to eBay ("thundering herd," not a correctness bug since refresh tokens are reusable, but wasted requests and avoidable throttling risk). `src/lib/ebay-oauth.ts` now dedupes concurrent refreshes per user via an in-flight-promise map, so simultaneous callers share one refresh instead of each starting their own.
  **Investigated and intentionally left alone:** `next/image` is not adopted anywhere in the app (`next.config.js`'s `images.unoptimized: true`, with its own comment) — this is a deliberate, still-relevant workaround for an unpatched Next.js 14.x image-optimizer DoS advisory (GHSA-9g9p-9gw9-jx7f), not an oversight, and switching it on is coupled to the already-deferred Next.js 15/16 upgrade, not a standalone fix. `/api/ebay/ship` and `/api/ebay/store` were already using `Promise.all`/worker-pool concurrency from earlier session work — no changes needed. `src/middleware.ts` runs a real Supabase query (`app_settings.deletion_requested_at`) on every authenticated request (every page nav and every API call) to enforce the pending-deletion lock — the single highest-traffic per-request cost in the app, but deliberately not touched here since a caching fix would sit right next to the account-deletion security boundary; flagged as the top remaining recommendation, not yet implemented. `GET /api/drafts` has no `.limit()` on its query — fine at current row counts, worth adding pagination if/when a single account's draft history grows large.

- **Batch-upload throughput pass, Sept 3, 2026 — built for sellers listing 40+ items in one sitting.** A second, deeper performance audit (this time framed around "list 40 in a day fast," not just per-listing speed) found the save/upload path had the same one-item/one-photo-at-a-time pattern the earlier eBay pass had already fixed on the listing side:
  1. **`uploadThumbnail()` (`src/lib/storage.ts`) called `supabase.auth.getUser()` on every single photo upload** — a real network round trip to Supabase's Auth server, not a local read. A 40-item batch with a couple of photos each meant 100+ of these, all just to re-derive the same signed-in user id. Now resolved once and cached in memory for the rest of the page's life (safe because every sign-out in this app does a hard `window.location.href` navigation, which clears all JS state anyway) — including caching the in-flight promise so a batch's first wave of concurrent uploads share one lookup instead of each firing their own.
  2. **Per-item photo uploads were sequential.** Both `new-listing/page.tsx` (3 photo slots) and `batch-upload/page.tsx` (up to `MAX_PHOTOS_PER_ITEM` = 6 per item) uploaded their photos one at a time in a `for` loop before saving the draft. Both now fire all of an item's photo uploads concurrently via `Promise.all` — order is preserved in the results array regardless of which upload actually finishes first, so "front photo becomes the thumbnail" logic is unaffected.
  3. **"Save all drafts" (`handleSaveAllDrafts` in `batch-upload/page.tsx`) saved one item at a time**, unlike every other bulk action on this page (analyze, price, list) which already use a small worker-pool pattern. It's now a 5-way concurrent pool — higher than the eBay-touching bulk actions (2-3) since this step is pure Supabase reads/writes with no external rate limit to respect.
  Net effect for a real 40-item batch: dozens of previously-serial round trips (auth lookups, photo uploads, draft saves) now run concurrently, on top of the eBay-side fixes from the earlier pass (parallel SKU cleanup, cached merchant location, deduped token refresh) that already sped up the "List all on eBay" step.
  **Investigated, not changed:** the photo-grouping step (`handleGroupPhotos`) chunks uploads at 15 photos with a 1.5s delay between chunks — this is a correctness requirement, not laziness (a group can span a chunk boundary, so each chunk's last group has to carry over as "pending" into the next call), and only ever applies past 15 photos in one batch. The eBay Browse API app-token cache in `pricing/suggest/route.ts` isn't deduped against concurrent cold-start requests the way `ebay-oauth.ts`'s user token cache now is — left alone since the token lives ~2 hours and the worst case is a couple of redundant token fetches once per that window, not a per-item cost.
  **Flagged then as a workflow gap, since closed:** at the time this was written there was no "apply to all" bulk-edit on the batch-upload results screen. Condition + heavy-item shipping got one Sept 3, 2026 (see the "Bulk-edit on batch-upload results" bullet below), and store category joined it Sept 9, 2026 (see "Bulk-apply store category"). No real per-item shipping *mode* selector exists anywhere in the app yet (still just the Heavy checkbox — see the Shipping mode bullet above), so there's nothing to bulk-apply there either.
- **Photo cap raised 100 -> 200, Sept 9, 2026.** `MAX_PHOTOS` in `src/app/batch-upload/page.tsx` was too tight for the app's own "list 40 items in a session" goal: 40 items x even a modest 3 photos each is 120, past the old cap before a seller could finish uploading, and the item-divider marker photos (shipped earlier this session) make that worse, not better, since a marker adds one more photo per item on top of the real ones. Raised to 200 (40 items x up to 5 each, comfortable headroom under `MAX_PHOTOS_PER_ITEM`). Nothing downstream needed to change — grouping/analysis/save/listing were already chunked or concurrency-pooled rather than looping the whole batch in one shot, so a bigger batch just takes longer, it doesn't break. The one user-facing addition: past `LARGE_BATCH_NOTICE_THRESHOLD` (60) photos, the upload screen shows a heads-up that AI grouping will take a few minutes across several chunked rounds (and that tapping manual dividers instead skips that wait entirely) — a soft notice, not a block, so a seller isn't blindsided mid-upload the way the old hard 100-photo cap could blindside them instead.
- **Store category suggestions, shipped Sept 9, 2026 — closes the gap flagged Sept 3.** Every seller now gets a store category suggestion automatically wherever they review an item (`batch-upload`'s results screen, `drafts/[id]`, and `new-listing`), with no setup and no AI cost:
  1. **Free keyword match (`src/lib/store-category-match.ts`), on for everyone, always.** Client-side, no network call beyond the one `GET /api/ebay/store-categories` fetch per page load. Suggests a category only when every significant word in that category's own name shows up in the item's title/type/brand (with a naive singular/plural fold) — deliberately strict, since a wrong auto-pick silently mis-filing an item into the wrong shelf of someone's storefront is worse than suggesting nothing. Picks the most-specific (most-worded) matching category when more than one qualifies.
  2. **Optional AI suggestion, Settings -> "Store category suggestions" -> AI suggestions, off by default (`supabase-migrations/015`).** One extra text-only `gpt-4o-mini` call per item (`POST /api/ebay/store-categories/suggest`, no photos needed) that picks from the seller's own real category list — never an invented one, validated server-side against `fetchStoreCategories()`'s live list — and overrides the keyword guess when it finds a better fit. Costs against the account's monthly AI usage cap, gated both by the Settings toggle client-side and re-checked server-side (a stale client can't spend AI usage the account didn't consent to). Bulk batch-upload path runs this 3-at-a-time (`STORE_CATEGORY_AI_CONCURRENCY`), same worker-pool pattern as analysis/pricing — still an OpenAI call per item, no reason to fire up to 200 at once.
  3. **Manual override, always available.** A category chip next to each item's SKU field (both screens) shows the current suggestion and opens a dropdown of the seller's real categories on tap — picking one there always wins, whatever the free/AI suggestion said.
  Selecting nothing is a valid, unsurprising choice: with no eBay Store Categories set up (or eBay not connected), the whole picker just doesn't render on any of the three screens, and a draft with no category still lists fine — `setListingStoreCategory()` in `ebay-inventory.ts` was already best-effort and non-blocking. `new-listing/page.tsx` has no SKU field to sit the chip "next to" (unlike the other two screens) — it sits in the "AI detected" card instead, below the brand/color/size grid, and re-suggests fresh on every "Analyze & price" run the same way brand/color/size already do.


- **Batch-upload item dividers, shipped Sept 9, 2026 — two free/deterministic alternatives to AI-similarity photo grouping.** Built after a session walkthrough of how real sellers already shoot batches (many photograph in strict per-item order and end each item with a shot of a hand-written/printed number — a card, a tag, the outside of a poly bag/mailer — to keep items straight). Two additions to `src/app/batch-upload/page.tsx`'s `handleGroupPhotos`, both checked before the existing AI-similarity grouping (`/api/group-photos`) ever runs:
  1. **Manual dividers (always available, zero AI cost).** A Scissors toggle button on each photo thumbnail on the upload screen lets the seller tap "this is my SKU/number photo -- end this item here." Any non-empty set of taps (`manualDividers`) always wins — `buildGroupsFromManualDividers` cuts the photo list at those exact points, entirely client-side, no network call. **Fixed Sept 9, 2026:** the tapped photo used to be kept as that item's last listing photo; it's now excluded, matching the AI-detected-marker path below, so a seller's queue-card/bag shot can never get posted by accident just because they tapped it instead of relying on auto-detect.
  2. **Auto-detect from a numbered marker photo (opt-in, Settings → "Batch upload: item dividers" → On, off by default — `app_settings.auto_detect_item_dividers`, migration `013_auto_detect_item_dividers.sql`).** When on and no manual dividers are set, the client sends photo thumbnails to the new `POST /api/detect-item-dividers` (chunked the same way as `/api/group-photos`, but — unlike similarity grouping — each chunk is an independent per-photo classification with no "last group carries into next chunk" dependency, so chunks run concurrently instead of strictly sequentially). It flags which photos are number markers and reads the digits/code off each. `buildGroupsFromMarkers` then cuts the photo list at those points, this time EXCLUDING the marker photo itself from the group it closes (it's a throwaway queue-card/bag shot, never meant to be a listing photo) and returns which item each marker's code belongs to, which pre-fills that item's existing optional SKU field (`customSkus`) automatically. If the setting is on but a given batch has no marker photos, it falls through to the AI-similarity grouping below rather than leaving the seller with nothing. Consumes one AI call per chunk against the monthly AI usage cap, same as grouping.
  Net effect: a seller who shoots in the divider-card style never needs AI to guess item boundaries at all — grouping becomes free, instant, and can't be mis-grouped, and their queue numbers become the item's SKU for free too.
- **AI usage cap: one clear banner instead of repeated per-item messages, fixed Sept 9, 2026.** A gap found while reviewing the just-shipped 400→3,000 cap raise: when `/api/analyze-batch` hits the monthly cap partway through a batch, every affected item's card showed the same "You've reached this month's AI usage limit" text individually — no single summary, easy to miss on a large results screen, and no reason it should repeat verbatim N times. `handleAnalyzeBatch` in `src/app/batch-upload/page.tsx` now also counts how many items in the just-finished batch failed for that specific reason and, if any did, sets the page's existing top-of-screen error banner with a one-line summary ("N items below couldn't be analyzed... retry once it resets on the 1st"). The per-item cards and their individual Retry buttons are unchanged — this only adds the missing at-a-glance summary on top.

- **75%-of-cap usage warning banner, shipped Sept 9, 2026 (extended same day to cover batch-upload).** The AI usage cap (Phase 3 bullet above) previously only surfaced once a seller actually hit it, mid-analysis, with no earlier heads-up anywhere in the app. `GET /api/ai-usage` (`src/app/api/ai-usage/route.ts`) does a read-only lookup of the caller's own `ai_usage_counters` row (migration 014, see above) and returns `{used, limit}` -- treating a row whose `period_month` isn't the current calendar month as `used: 0`, since `check_and_consume_ai_usage` only actually rolls that row over the next time it runs, not automatically at midnight on the 1st. The fetch-and-threshold logic lives once in `src/lib/use-ai-usage-warning.ts` (`useAiUsageWarning()`, threshold 0.75) so every place that shows the banner stays in sync on wording. Two call sites: `src/components/BottomNav.tsx` renders it floating directly above the bottom nav pill on every page that has that nav (dashboard, store, drafts, membership); `src/app/batch-upload/page.tsx` -- which doesn't render `BottomNav` at all -- renders its own copy inline near the top of the page (same spot as the page's existing error banner), right where the AI calls this cap actually limits are being made. Both are purely informational, no dismiss control -- they just stop showing once usage drops back under 75% (i.e. the next month rolls over).

- **eBay publish delay made conditional instead of unconditional, Sept 9, 2026.** `/api/ebay/list/route.ts` had three hardcoded `setTimeout` waits ("brief pause so eBay's inventory service indexes the item") between creating something and immediately referencing it. The main one -- 1500ms between `createOffer` succeeding and `publishOffer` being called -- ran on every single listing regardless of whether eBay actually needed the wait, a flat tax on the app's most-used action across a 40-item session. Changed to try-first-then-retry: `publishOffer` is attempted immediately, and only if that first attempt fails does the route wait 1500ms and retry the identical call once before falling into the existing (unchanged) error-classification logic below it. Same treatment applied to the matching 1500ms wait before `createOffer` inside the category-fallback retry loop. Worst case (eBay genuinely always needs the full 1500ms) costs one extra failed round trip versus before; best case -- most listings, if real indexing time is usually under that -- costs nothing. **Deliberately left alone:** the third wait (2000ms after `deleteOffer`, before the fallback loop's first `upsertInventoryItem` attempt) -- that loop tries several conditions and just `continue`s past whichever attempt fails, with no way to tell "this condition is genuinely invalid" apart from "the delete hasn't propagated yet," so removing it risks silently burning through every entry in `conditionsToTry` on a listing that would have worked fine with the wait. Also a much rarer path (only reached once the first publish attempt already failed for a real reason), so the payoff for optimizing it further is small. Verified clean `tsc`/`eslint` only -- not exercised against live eBay from this pass, since doing so would create/modify a real listing on the connected account; Sel should watch the next real batch's listing speed and error rate to confirm in practice.

- **Sales-page error messages made plain-English, plus a real reconnect-detection gap closed, Sept 9, 2026.** Sel saw a raw eBay OAuth scope error on screen after a real "not-yet-reconnected eBay" situation (expected after adding the `sell.finances` scope earlier the same day) — confirmed a false alarm, not a code bug, once Sel reconnected. But investigating it surfaced one real, worth-fixing gap: `GET /api/ebay/sales`'s `isAuth` keyword check (meant to trigger a "reconnect eBay" hint) didn't match the word "scope" — eBay's actual error text ("...exceeds the scope granted to the client") wouldn't have shown a reconnect prompt even on a genuine scope mismatch. Broadened the check to include "scope", added server-side `console.error` logging of the real eBay error text, and replaced the raw error text shown to Sel with a plain-English message (a reconnect prompt when it looks like an auth/scope issue, a generic "try again" otherwise).
- **Standing plain-English backlog file, added Sept 9, 2026.** New `BACKLOG.md` at the repo root — separate from this file, no jargon, one entry per deferred item explaining the problem and the fix in plain terms. This file (CLAUDE.md) stays the technical/AI-context doc; `BACKLOG.md` is the one to hand Sel. Going forward, every future deferred item gets added to both.
- **Stale-listing nudges on Store, shipped Sept 9, 2026.** Active listings sitting unsold a long time were easy to miss in a long list. `src/app/store/page.tsx`: `STALE_DAYS_THRESHOLD = 30`, computed from the `startTime` every active listing already carries (zero new eBay API calls). A small "Stale" pill badge renders next to any Active-tab row past the threshold, plus a toggle chip in the existing search/sort bar ("N stale (30+ days, no sale)" / "Showing stale only") that narrows the existing `filtered` list the same way search already does — Select-mode/bulk-price-update need no changes since they already operate on whatever's in `filtered`. Deliberately left out: enriching the flag with eBay's watch-count, which needs a heavier `GetMyeBaySelling` payload whose speed impact isn't tested yet — logged in `BACKLOG.md` instead of shipped half-verified.
- **Sourcing insights (by-category breakdown) on Sales, shipped Sept 9, 2026.** No visibility into which kind of item was actually worth sourcing. `GET /api/ebay/sales` now also joins `item_type`, `store_category_name`, `created_at` from the existing `drafts` lookup (no new round trip) onto each sale as `itemType`/`storeCategoryName`/`draftCreatedAt`. New `src/lib/insights.ts` (`groupSalesByCategory`, pure function, 7 Vitest tests) groups by `storeCategoryName ?? itemType ?? "Uncategorized"` and computes count/revenue/margin%/avg-days-to-sell per group — margin only over the sub-set of sales that actually have a cost entered (never diluted by unknown-cost sales), avg-days-to-sell only over sales with a valid, non-negative listed→sold gap (guards against a relisted item whose draft is much older than its real listing date). `/sales` renders it as a new table above the existing revenue chart, with a caveat line that days-to-sell is approximate (based on when the item was added, not always exactly when it went live).
- **Buyer questions in-app (view + reply), shipped Sept 9, 2026.** Sellers previously had to leave the app to see or answer a buyer's "ask seller a question." Built against eBay's Trading API docs (`GetMemberMessages`/`AddMemberMessageRTQ`, fields verified before writing code, not assumed): new `src/lib/ebay-messages.ts` — `fetchUnansweredQuestions()` calls `GetMemberMessages` with `MailMessageType=AskSellerQuestion`, `MessageStatus=Unanswered`, and a trailing-30-day creation-date range (not scoped to one listing), parsing `Question` blocks into `{messageId, senderId, subject, body, createdAt, itemId}`; `respondToQuestion()` calls `AddMemberMessageRTQ` with `Body`/`ParentMessageID`/`RecipientID`, defaulting `DisplayToPublic` to `false` (eBay's own default isn't confirmed either way, and a casual reseller likely doesn't expect a reply to post publicly by default). `GET /api/ebay/messages` is a single call covering every listing at once (unlike `GetBestOffers`, no per-listing worker-pool needed) and joins title/thumbnail from `ActiveList` the same way `/api/ebay/offers` does. `POST /api/ebay/messages/respond` validates the reply body (1–2000 chars, matching eBay's own limit). New `src/app/messages/page.tsx` mirrors `/offers`'s card-list UI, with `window.confirm(...)` before every reply (irreversible buyer communication). Entry-point "Buyer questions" cards added to both `/store` and `/dashboard` (dashboard also shows a live unanswered count, same cost class as the existing "Pending offers" count). **Notably, unlike every previous eBay feature added this session, no new OAuth scope or reconnect is needed** — the base `https://api.ebay.com/oauth/api_scope` scope (already in `EBAY_SCOPES`) is confirmed sufficient. **What this pass could and couldn't verify:** `tsc`/`eslint` clean, all 53 Vitest tests passing, dev-server smoke test confirming `/messages` and both new routes return 401/307 while signed out (matching every other auth-gated route). Could NOT verify: whether a real reply posted through `AddMemberMessageRTQ` actually reaches a real buyer correctly — no way to generate a real buyer question to test against from here, same limitation as Best Offer before it. Sel should try it against a real question once one comes in.

- **Heavy-item/shipping-cost flag no longer lost on "Save draft," migration 018.** Found during a full workflow review: `new-listing`'s and `batch-upload`'s save-draft payloads never sent `isHeavy`/`shippingCost` to `POST`/`PATCH /api/drafts` -- those fields only ever reached `POST /api/ebay/list` at the moment of immediate listing, or (on `drafts/[id]` specifically) `localStorage` alone. A heavy item saved as a draft and listed later (from `/drafts`'s bulk "List selected," which built its heavy-item set from `localStorage` only) silently lost the flag and listed with the wrong shipping. `drafts.is_heavy`/`drafts.shipping_cost` (migration `018_draft_heavy_shipping.sql`) now persist it properly; `POST /api/drafts` and `PATCH /api/drafts/[id]` validate and store both; all three save-draft payloads (`new-listing`, `batch-upload`, `drafts/[id]`'s `handleSave`/`handleList`) now send them; `GET /api/drafts` (list) returns them so `/drafts`'s bulk-list flow reads the draft's own saved value first, falling back to the old `localStorage` cache only for a draft saved before this migration.
- **`storeCategoryWarning` (already returned by `POST /api/ebay/list` when the post-publish category call fails) now actually shown to Sel.** It existed in the API response already -- the comment there says as much -- but no page read it, so a listing whose store-category assignment silently failed gave no indication. `new-listing`, `batch-upload` (per item), and `drafts/[id]` all now display it as a small warning, same visual pattern as the existing "missing required aspects" warning next to it.
- **`/messages` reconnect detection added -- the one eBay page missing it.** Every other eBay page (`store`, `sales`, `ship`, `offers`) classifies an auth/scope error and shows "Reconnect eBay"; `/messages` (shipped earlier today) never did, so an expired token here dead-ended on a generic "try refreshing." `fetchUnansweredQuestions()` (`src/lib/ebay-messages.ts`) now runs the same `isAuth` keyword check as `sales/route.ts`, `messages/route.ts` returns a `reconnect` flag, and `messages/page.tsx` shows the same reconnect link every sibling page already has.
- **Re-analyze on `drafts/[id]` no longer discards the retaken photos.** `handleReanalyze` sent up to 3 new photos to `/api/analyze-item` purely to refresh text fields, then threw them away -- a seller retaking a flaw/measurement shot specifically to fix a draft reasonably expects it to become a listing photo. Now uploads them via the same `uploadThumbnail()` helper every other page uses and appends them to `photo_urls`.
- **Real bug found and fixed while auditing response shapes: `/store`, `/offers`, and `/messages` never actually showed their "Connect/Reconnect eBay" prompts.** All three returned a `502` (or, for messages, no classification at all) for an eBay-connection problem, carrying `{error, connect, reconnect}` in the body -- but `apiFetch()` (`src/lib/api.ts`) throws on any non-2xx status and discards the parsed body doing so, so the `if (data.error) { setNeedsConnect(...) }` code already sitting in every one of those pages was silently unreachable for this exact case; a disconnected seller saw a bare generic error with no "Connect eBay" link on any of these three pages, worse on `/store` specifically since a non-empty Supabase-cached listing set meant no error was shown at all. All three routes now return `200` with the same `{error, connect, reconnect}` shape `/api/ebay/sales` and `/api/ebay/ship` already used correctly -- no frontend changes needed beyond `/messages`'s new reconnect flag above, since the existing handling code in each page was already correct and just never ran.
- **`xmlFindAll` de-duplicated.** `offers/route.ts` and `ebay-messages.ts` each had their own copy of the same tag-array XML parser; moved into `src/lib/ebay-listings.ts` alongside `xmlFind` (whose own header comment already exists specifically to stop this kind of copy-paste), both call sites now import the shared one.
- **Dashboard "Stale listings" tile, and an early eBay-disconnect warning on batch-upload, Sept 9, 2026.** Two workflow-review gaps: (1) stale listings (30+ days active, no sale) only ever showed up if Sel happened to open `/store` -- every other "needs attention" state (ship, offers, questions) already had a Dashboard count tile, this one didn't. `STALE_DAYS_THRESHOLD`/`daysSinceDate`/a new `isStale()` helper moved out of `store/page.tsx` into new `src/lib/stale-listings.ts` so `/store` and the new Dashboard tile share one formula and can't drift apart -- Dashboard fires its own `/api/ebay/store` fetch (same page-cache pattern as its other tiles) and counts Active-tab listings past the threshold client-side. (2) `batch-upload` only discovered eBay wasn't connected after a full batch was uploaded/grouped/reviewed and "List on eBay" was clicked -- by then reconnecting means a full-page OAuth redirect that loses all in-memory review state. Full state-preservation across OAuth was out of scope; instead added a new, deliberately cheap `GET /api/ebay/connection-status` (one Supabase row lookup via the existing `requireEbayConnection()`, no eBay API call) that batch-upload now calls once on page load, showing a non-blocking "eBay isn't connected -- reconnect before listing, or these will stay as drafts" banner early instead of only at listing time.

- **Daily email digest ("something needs your attention"), shipped Sept 9, 2026 -- and a real, likely-never-firing-in-production bug found and fixed while building it.** Closes the gap flagged in the initial workflow review: previously the only way to find out about a sale to ship, a pending offer, a buyer question, or a stale listing was to open the app. Sel asked for the send time to be customizable (not a fixed daily time), which shaped the design:
  - Migration `019_notification_email_settings.sql` adds three `app_settings` columns: `notification_email_enabled` (bool, default true), `notification_email_hour_utc` (smallint 0-23, default 13), `notification_email_last_sent_at` (nullable timestamptz, dedup guard). Deliberately no IANA timezone column -- Settings' `<input type="time">` converts the seller's local clock to/from this one UTC integer client-side (`utcHourToLocalTimeString`/`localTimeStringToUtcHour` in `settings/page.tsx`, rounding to the nearest UTC hour), at the cost of the *displayed* local time potentially drifting by up to 30 min for half/quarter-hour-offset timezones and by exactly one hour across a DST transition -- judged simpler than a full timezone column for what a single-seller app's "roughly this time of day" needs.
  - Because the send time is per-account and there's no per-user job scheduler, `vercel.json`'s new `/api/cron/daily-digest` entry runs **hourly** (`0 * * * *`), not once a day like `purge-deleted-accounts` -- the route itself checks every eBay-connected user with the digest enabled and only actually emails the ones whose `notification_email_hour_utc` matches the current UTC hour, skipping anyone `notification_email_last_sent_at` says already got one in the last 20 hours (covers a duplicate trigger or a repeated local hour across a DST fallback).
  - Counts are pulled by calling the *existing* per-feature logic directly (not HTTP self-calls), same as the plan called for -- two of these needed extracting out of their routes first since they weren't already importable functions: `fetchPendingOffers()` moved from `offers/route.ts` into new `src/lib/ebay-offers.ts`, and `fetchUnshippedOrders()` moved (minus `ship/route.ts`'s own thumbnail/gallery-lookup enrichment, which a count never needed) into new `src/lib/ebay-ship-orders.ts` -- both routes now just call the shared function and wrap it in the same response shape they always returned (diffed to confirm byte-identical). `fetchUnansweredQuestions()` (`ebay-messages.ts`) and `isStale()` (`stale-listings.ts`) were already reusable from earlier this session. New `src/lib/supabase-service.ts` also extracts the service-role-client helper that used to be a private copy inside `purge-deleted-accounts/route.ts`, now shared by both cron routes.
  - New `src/lib/email.ts` wraps the `resend` npm package (added to `package.json`) with one `sendDigestEmail()` function -- plain text, not HTML, listing only the categories that are actually nonzero. Resend's free/sandbox tier (no verified custom domain) can only send to the account's own verified address, which matches Sel's single-seller case; the "from" address is Resend's own `onboarding@resend.dev` sandbox sender, needing no domain setup. `RESEND_API_KEY` was added directly to `.env.local` and to Vercel's project env vars by Sel -- deliberately never typed into this conversation or the source tree; the code only ever reads `process.env.RESEND_API_KEY`.
  - New Settings card ("Email alerts," between "Fee estimate" and "Account") -- on/off `OptionCard`s matching every other boolean setting's pattern, plus the time picker (shown only when on).
  - **The real bug**: while smoke-testing the new cron route locally, it -- and, it turned out, the *already-shipped* `purge-deleted-accounts` cron too -- returned `src/middleware.ts`'s own `{"error":"Not authenticated"}` even with a correct `Authorization: Bearer $CRON_SECRET` header attached, never reaching either route's own CRON_SECRET check at all. Root cause: `src/middleware.ts` requires a signed-in Supabase session for every `/api/*` path not explicitly carved out (by design, per that file's own comment -- a previous session deliberately closed a blanket-public-`/api` bypass), but Vercel Cron's actual triggered request carries no session cookie whatsoever, only the bearer token -- so the middleware's `!user` branch fired first on literally every cron-triggered request, on every scheduled run, since the day that middleware was rewritten to cover all of `/api`. In effect: **the daily account-deletion purge has very likely never once successfully run on its real Vercel-triggered schedule** (it would only have worked if someone manually curled it with the right secret). Fixed by adding `/api/cron` to `PUBLIC_PREFIXES` in `middleware.ts`, the same carve-out already used for `/api/ebay/callback` and `/api/auth/lockout` -- both of which do their own internal auth and were exempted for exactly this reason. Verified locally: both cron routes now correctly reach their own CRON_SECRET check (confirmed 401 with the routes' own body on a missing/wrong secret, and a clean caught error rather than a crash on a right secret but no local `SUPABASE_SERVICE_ROLE_KEY`), while every other `/api` route still correctly 401s with middleware's own message when signed out -- no blanket bypass reintroduced.
  - **What this pass could and couldn't verify:** `tsc`/`eslint` clean, all 53 Vitest tests passing, a full dev-server route sweep (pages 307, APIs 401, cron routes reaching their own auth logic) with no regressions across the whole app. Could NOT verify end-to-end from here: an actual send through Resend (needs the live `SUPABASE_SERVICE_ROLE_KEY`/`CRON_SECRET` that only exist on Vercel, plus a real eBay connection, neither available in this environment), or that the *now-fixed* purge cron actually fires correctly on Vercel's real schedule going forward. Sel should manually trigger `/api/cron/daily-digest` once deployed (a plain `curl` with the real `Authorization: Bearer $CRON_SECRET` header) to confirm a real email arrives, and can spot-check the next scheduled purge-cron run in Vercel's cron logs.

- **Dashboard/Offers/Messages performance pass, Sept 9, 2026 (found during a speed/efficiency review Sel asked for).** One Dashboard load fanned out to `/api/dashboard/stats`, `/api/ebay/offers`, `/api/ebay/messages`, and `/api/ebay/store` nearly simultaneously, and three of those four each independently called `fetchAllOfListType("ActiveList")` -- meaning a single dashboard view could mean 3+ near-duplicate `GetMyeBaySelling` round trips to eBay at once, all for the same "every active listing" snapshot. Fixed in `src/lib/ebay-listings.ts`: `fetchAllOfListType` is now a cached wrapper around the renamed `fetchAllOfListTypeUncached`, keyed by `userId:listType`, 15s TTL, with concurrent callers sharing one in-flight request instead of each firing their own -- same "thundering herd" shape and fix as `getAccessToken()` in `ebay-oauth.ts`. Failures are never cached, so a transient eBay error isn't replayed to every other caller for the next 15s. Separately, `/offers` and `/messages` never used the app's existing instant-paint `page-cache` module (already used by Dashboard/Store/Drafts/Sales), so every visit showed a blank spinner even right after a prior visit -- wired both pages into `getPageCache`/`setPageCache` the same way `store/page.tsx` already does (`load()` now takes a `{ silent? }` option so the mount call can skip forcing the spinner when cached data exists, while the manual refresh button still shows it, unchanged). Verified: `tsc`/`eslint` clean, all 53 Vitest tests passing, dev-server route sweep (all four pages still resolve, no regressions).
  - **Follow-up bug found reviewing that same commit (not yet pushed), Sept 10, 2026.** `/offers` and `/messages` `load()` cleared the list to empty (`setOffers([])`/`setQuestions([])`) on *any* `data.error` response -- but that branch fires for routine eBay hiccups too, not just a real disconnect (`/api/ebay/offers` and `/api/ebay/messages` both return `200 {error, connect, reconnect}` for a plain transient failure, same shape as the disconnect case). Since `load()` now also runs silently on every revisit inside the page-cache window, a passing eBay error during that silent refresh wiped a perfectly good cached list and left only an error toast -- worse than doing nothing, and the opposite of what the cache was added for. Removed the list-clearing in both pages' error branch so a transient failure leaves the last-known list on screen with the error banner over it, matching the fallback reasoning `store/page.tsx` already uses for its own eBay failures. `tsc`/`eslint` clean, all 53 Vitest tests still passing.
  - **Formal code-review pass on the same change, Sept 10, 2026 -- four follow-up fixes.** 1) `/offers` and `/messages` `load()` had a latent race: the silent auto-load fired on mount and a manual refresh-button click could now overlap (e.g. tapping refresh right after landing on a cached page), and with no request sequencing a slower, older response could land after and overwrite a newer one (or clear the loading spinner while a real request was still in flight). Both pages now stamp each `load()` call with an incrementing id and discard any response whose id no longer matches the latest call. 2) `fetchAllOfListType`'s TTL cache (previous bullet) only ever overwrote entries, never removed one whose TTL had elapsed -- harmless at today's user count, but unbounded if that ever changes. Extracted the whole cache+in-flight-dedup shape into a new generic `src/lib/ttl-cache.ts` (`createTtlDedupeCache`) that sweeps expired entries on every successful write, and rewired `fetchAllOfListType` to use it -- same behavior, now with an eviction guarantee instead of an implicit one. 3) That extraction also made the caching/dedup logic testable without mocking network calls or eBay request context, which the app's Vitest suite deliberately stays out of (see the Automated Tests bullet below) -- new `src/lib/ttl-cache.test.ts` (7 tests) covers TTL reuse/expiry, concurrent callers sharing one in-flight fetch, and a failed or vetoed (`shouldCache`) result never getting cached. 4) `src/lib/ebay-ship-orders.ts` carried its own third local copy of `xmlFind`/`xmlFindAll` -- now imports the shared versions from `ebay-listings.ts`, same cleanup already done for `offers/route.ts` and `ebay-messages.ts`. Verified: `tsc`/`eslint` clean, all 60 Vitest tests passing (53 existing + 7 new), a dev-server route sweep (all six pages 307, all three API routes 401 while signed out, no regressions, no 500s).

### Supabase drafts table columns
id, user_id (uuid, FK to auth.users, RLS-scoped — see Phase 1 note above), title, brand, color, size, condition, flaws, suggested_price, avg_sold, sell_odds, thumbnail_url, custom_sku (unique per user_id, not globally), item_type, style, material, theme, sleeve_length, neckline, fit, pattern, description, ebay_listing_id, photo_urls, vintage, character, character_family, year_manufactured, season, store_category_id, store_category_name, created_at

### Supabase app_settings table
One row per user (`user_id uuid primary key references auth.users(id)`, as of Phase 1 — previously a singleton `id=1` row shared by everyone). Columns: `user_id`, `default_shipping_mode` (`'free' | 'calculated'`), `store_description_footer` (nullable text, added by migration 011 — see the bullet below), `auto_detect_item_dividers` (boolean, default false, added by migration 013 — see the item-dividers bullet below), `ai_store_category_suggestions` (boolean, default false, added by migration 015), `ebay_fee_percent_override` (nullable numeric, added by migration 016), `notification_email_enabled` (boolean, default true, added by migration 019 — see the email-digest bullet below), `notification_email_hour_utc` (smallint 0-23, default 13, added by migration 019), `notification_email_last_sent_at` (nullable timestamptz, added by migration 019), `deletion_requested_at` (nullable timestamptz, added by migration 008 — set by `POST /api/account/delete`, cleared by `POST /api/account/reactivate`, read by the purge cron), `updated_at`. `/api/settings` GET falls back to `"free"`/empty string if a user has no row yet; PATCH accepts any field independently and upserts (`onConflict: "user_id"`) so a brand-new account's first save creates its row.

### Supabase ebay_connections table
One row per user, added in Phase 2 (`supabase-migrations/004_ebay_per_user_connections.sql`). Columns: `user_id` (uuid PK, FK to `auth.users`), `ebay_user_id`, `encrypted_refresh_token` (AES-256-GCM, `src/lib/ebay-token-crypto.ts` — never stored or logged in plaintext), `shipping_free_policy_id`, `shipping_heavy_policy_id`, `shipping_calculated_policy_id`, `return_policy_id`, `connected_at`, `updated_at`. Written by `/api/ebay/callback` on connect; read by `requireEbayConnection()` (`src/lib/ebay-connection.ts`) at the top of every eBay-touching route; policy columns updated by `/api/ebay/policies` PATCH from Settings' pickers. RLS scoped to `auth.uid() = user_id`, same pattern as `drafts`/`app_settings`.

### Supabase login_attempts table
Brute-force login tracking, added by `supabase-migrations/005_login_lockout_protection.sql`. Columns: `email` (text PK, lowercased before write), `failed_count`, `locked_until`, `last_attempt_at`. RLS enabled with **no policies at all** — this table is never touched directly, only through the three SECURITY DEFINER functions (`check_login_lockout`/`record_failed_login`/`clear_login_attempts`), which since `006_lock_down_login_rpcs.sql` are grantable to `service_role` only — called from `POST /api/auth/lockout`, never directly from the browser. See the Security hardening bullet above.

### Supabase ai_usage_counters table
One row per user, added in the Phase 3 spending-safety-net work (`supabase-migrations/010_ai_usage_cap.sql`). Columns: `user_id` (uuid PK, FK to `auth.users`), `period_month` (date, first-of-month; rolled over automatically by the RPC below), `call_count`, `updated_at`. RLS enabled; `call_count` can still only ever be *changed* through the SECURITY DEFINER function `check_and_consume_ai_usage` (`service_role` only, called from `src/lib/ai-usage.ts`) — no insert/update/delete policy exists. **Migration `014_ai_usage_read_policy.sql`, added Sept 9, 2026,** grants one read-only exception: a signed-in user can `select` their own row (`(select auth.uid()) = user_id`), so `GET /api/ai-usage` can read it with the caller's own client instead of a service-role proxy. See the Phase 3 bullet above and the usage-warning-banner bullet below.

- **Error monitoring via Sentry, added Sept 3, 2026.** Free "Developer" tier (one user, error monitoring, email alerts — plenty for this app's scale). Deliberately minimal setup: `sentry.client.config.ts`/`sentry.server.config.ts`/`sentry.edge.config.ts` (browser, Node routes, and `src/middleware.ts`'s edge runtime respectively) all just call `Sentry.init({dsn, tracesSampleRate: 0})` — no performance tracing, and Session Replay was never enabled (this app's data — draft photos, shipping addresses — has no business being screen-recorded by a third party). `src/instrumentation.ts` is the Next.js hook that loads the server/edge configs; `src/app/global-error.tsx` catches anything that escapes every other error boundary and reports it before showing a plain "something broke" screen. `next.config.js` wraps the export with `withSentryConfig` (no `org`/`project`/`authToken` — those are only needed for source-map upload, a later optional upgrade, not something error capture depends on) and — important — had to add Sentry's ingest host to the CSP's `connect-src`, since the security-hardening pass's own lockdown would otherwise silently block the browser from ever reaching Sentry. `src/app/api/sentry-test/route.ts` is a standing, deliberately-broken route (`force-dynamic` so it doesn't fail the build trying to statically prerender something that always throws) for confirming events actually arrive — visit it once after deploying, check the Sentry dashboard, safe to leave in permanently.
- **New Listing / Batch Upload stale-draft bug, fixed Sept 3, 2026.** `/api/ebay/list` always builds the actual eBay listing from the draft's current row in the database, never from the request body — so anything that changes on screen after the draft was first saved only reaches eBay if it also reaches the database. `new-listing/page.tsx`'s `handleSaveDraft()` and `batch-upload/page.tsx`'s per-row `handleSaveDraft(index)` both used to short-circuit (`if (savedDraftId) return savedDraftId;` / `if (draftIds[index]) return draftIds[index];`) once a draft existed, turning every later call into a no-op — new-listing's "List on eBay" at least re-sent a narrow 6-field PATCH (title/brand/size/color/condition/price) on a repeat attempt, but batch-upload's didn't even do that. Either way, photos, AI-detected item type/description/style/material/etc., and (in new-listing) any field outside that narrow list were silently dropped: re-running Analyze, adding a photo, or editing a batch-upload row (title/condition/brand/color/size/flaws are all inline-editable there) after an initial save — including the very first "List on eBay" attempt if it failed and needed a retry, e.g. because eBay wasn't connected yet — meant the live listing shipped with stale, pre-edit data. Fixed by removing the short-circuit in both files: `handleSaveDraft` now always does a real sync (`POST` once to create, `PATCH` every subsequent call with the full current field set), and `handleListOnEbay` in both files now always calls it before listing instead of conditionally re-sending a subset. `drafts/[id]/page.tsx` (the edit-existing-draft screen) never had this bug — it always PATCHes its full field set before every "List on eBay" call, since it doesn't have a create-vs-update branch to short-circuit.
- **Store description footer, added Sept 3, 2026.** Settings → "Store description" is a free-text box (`app_settings.store_description_footer`, migration `011_store_description_footer.sql`) for a seller's standing boilerplate — welcome message, shop policies, a sign-off — written once instead of retyped into every listing. It's applied automatically and invisibly: `upsertInventoryItem` (`src/lib/ebay-inventory.ts`) appends it to the item's own description only at the moment the actual eBay listing body is built, joined with a blank line. It is never written into `drafts.description` itself, so an item's own description field always stays just that item's own details on every listing screen (new-listing, batch-upload, drafts/[id]) — none of them needed to change, since they all funnel through this one function via `/api/ebay/list`. The footer is looked up fresh on every "List on eBay" call (`/api/ebay/list/route.ts` reads `app_settings` alongside the existing default-shipping-mode lookup, one query for both), so editing or clearing it in Settings takes effect on the very next listing, not just future ones.

- **Estimated fee/net-profit tracking on Sales page, shipped Sept 9, 2026.** `/sales` used to show gross revenue only -- no visibility into what a seller actually keeps after eBay's cut. This app's eBay integration is Trading-API-based (`GetSellerTransactions`) and was never granted the `sell.finances` OAuth scope that would provide eBay's real per-order fee data, so this is explicitly an ESTIMATE, labeled as one everywhere it appears (summary card, per-sale row, Settings description) rather than presented as a real invoice figure. `src/lib/ebay-fees.ts` holds the shared math: `EBAY_STANDARD_FEE_PERCENT = 13.6` (eBay's published standard final value fee for most categories, verified via web search Sept 9, 2026 -- real rate varies by category/Store tier/promotions) plus a tiered per-order fixed fee ($0.30 at or under $10, $0.40 above). `GET /api/ebay/sales` now computes each sale's `estimatedFee` server-side and returns `totalFees`/`netRevenue`/`feePercent` alongside the existing `sales`/`totalRevenue`. A seller who knows their real effective rate differs can override it in Settings -> "Fee estimate" (`app_settings.ebay_fee_percent_override`, migration `016_ebay_fee_percent_override.sql`, nullable numeric -- null falls back to the standard rate); the field validates 0-100 both client- and server-side. **Deliberately scoped out:** COGS (cost-of-goods) tracking -- true net *profit* would need a per-item cost basis, which doesn't exist anywhere in the data model and would need a new `drafts` column plus UI across all three listing screens, comparable in size to the store-category work above. Flagged as the natural next step, not attempted alongside this round.
- **CI: lint + typecheck on every push/PR, added Sept 9, 2026.** The app had zero automated checks outside a developer's own machine -- directly implicated in the `globals.css` regression documented above (a silently-broken file wasn't caught until a user noticed dark mode was gone). `.github/workflows/ci.yml` runs `npm ci` + `npm run lint` (ESLint) + `npx tsc --noEmit` on every push and PR to `main`. **Deliberately does not run `npm run build`** -- a real Next.js production build needs this app's actual secrets (Supabase, OpenAI, eBay OAuth env vars), none of which exist as GitHub Actions repo secrets, so a build step would likely fail for reasons unrelated to whether the code is actually correct and produce a misleading red CI status; lint + typecheck catch the same class of regression (bad imports, type errors, unused/broken references) without needing any of that. Confirmed both commands exit 0 against the current repo before this shipped. Test coverage itself (there are still zero test files or test framework in the repo) is a separate, larger gap -- not attempted in this pass.

- **Automated tests (Vitest), added Sept 9, 2026 -- and a real bug found in the process.** `vitest` (pinned at `2.1.9`, not the newer `5.0.0` -- that version's peer deps want `@types/node >=24`, which conflicts with this repo's intentionally-pinned `@types/node ^20.12.12` and crashed npm's resolver entirely; `4.1.11` also worked but `2.1.9` was chosen for its much smaller peer-dependency surface, since nothing here needs Vitest's browser-mode features) with a `node`-environment config (`vitest.config.ts`, including a manual `@/*` alias -- Vite doesn't read `tsconfig.json`'s `paths` on its own) and `npm test` (`vitest run`). Scoped deliberately to pure-function unit tests of the app's money/matching logic -- `src/lib/{ebay-fees,store-category-match,shipping,pricing,sales-buckets}.test.ts`, 40 tests, no network/Supabase/eBay calls needed -- not component or e2e tests, which are a reasonable later addition but weren't needed to close the highest-value gap (silent logic regressions in code CI's lint+typecheck can't catch). Wired into `.github/workflows/ci.yml` as a third step after lint/typecheck. **Writing the store-category-match tests surfaced a real, already-shipped bug**: `normalizeWord()`'s naive plural-stripping (`"dresses" -> "dress"` by chopping the last 2 characters whenever a word ended in "es") also fired on words that are already singular and simply end in "e" -- "Purses" got mangled to `"purs"` (should fold to `"purse"`) and "Hoodies" to `"hoodi"` (should be `"hoodie"`), and separately, a plain singular word ending in "s" (e.g. an item titled "Dress") got its trailing "s" stripped too (`"dres"`), so it no longer matched its own plural category name (`"Dresses"` -> `"dress"`) at all -- meaning the store-category matcher (shipped Sept 9) could silently fail to suggest a category for two of the most common real eBay Store Category naming patterns for a clothing resale store (`"Dresses"`, `"Purses"`, and anything similar). Fixed in `src/lib/store-category-match.ts` by replacing the single-guess stemmer with a small variant-set approach: each word normalizes to a *set* of plausible forms (as-is, minus a trailing "s", minus a trailing "es") instead of one canonical guess, and a category word is considered matched if *any* of its variants overlaps *any* of the item's -- sidesteps the fact that English plural/singular can't be reliably disambiguated from the surface string alone without a dictionary (`"purses"` and `"dresses"` both end in the literal substring "es" but need different stripping rules). Verified against the specific failing pairs (`Dress`/`Dresses`, `Hoodie`/`Hoodies`, `Purse`/`Purses`) plus true sibilant `-es` plurals (`Box`/`Boxes`, `Watch`/`Watches`) and a negative case (`Shirt` vs. `Shorts`, confirming the fix doesn't introduce false matches) -- all now covered by the new test file so this can't silently regress again.

- **Bulk-apply store category on batch-upload, shipped Sept 9, 2026 -- closes the last gap the bulk-edit selection bar was missing.** The Sept 3 bulk-edit addition (condition + heavy-item shipping across a selection) explicitly left store category out because that picker didn't exist yet; it shipped six days later, but never got wired into the selection bar itself, leaving it the one per-item-only field on a screen otherwise built for "list 40 in a session." `src/app/batch-upload/page.tsx`: a `bulkStoreCategoryId` string (the picked category's id -- a `<select>` needs a string value, resolved back to the real `StoreCategoryLite` from `storeCategories` at apply time) and `applyBulkStoreCategory()`, mirroring `applyBulkCondition()`/`applyBulkShipping()` exactly -- same `getSelectableIndices().filter(i => selected[i])` target set (unsaved items only), same guard (`storeCategories.length > 0`, matching the per-item chip's own guard) hiding the control entirely for a seller with no eBay Store Categories set up.

- **COGS (cost-of-goods) tracking / true profit, shipped Sept 9, 2026 -- closes the gap flagged in the fee/net-profit bullet above.** `drafts.cost_basis` (nullable `numeric(10,2)`, migration `017_draft_cost_basis.sql`) is seller-entered only -- nothing in the app can infer what someone paid for a thrifted/sourced item, so it's always optional and defaults to null. `src/lib/profit.ts` holds the small pure logic (`isValidCostBasis`, `summarizeCost`), covered by its own Vitest tests. `POST /api/drafts` and `PATCH /api/drafts/[id]` validate and persist `costBasis` -> `cost_basis` the same way every other numeric field on those routes does. `GET /api/ebay/sales` now joins `cost_basis` onto its existing per-sale drafts lookup (no new round trip) and returns `trueProfit` (`netRevenue - totalCost`), `itemsWithCost`, and `itemsMissingCost` alongside the existing fee/profit fields. All three listing screens (`new-listing`, `batch-upload` per-item, `drafts/[id]`) got an optional "Cost (what you paid)" input next to their existing price field, wired into their save payloads as `costBasis` -- `drafts/[id]/page.tsx` needed the field added to all four of its separate PATCH payload sites individually (that file previously had an indentation bug from exactly this kind of repeated-field edit, so each occurrence was diffed on its own rather than trusted to one blanket find-and-replace). `/sales`'s summary card shows a third "True profit (after fees & cost)" stat, only once `itemsWithCost > 0` (a seller who's never entered a cost sees the page exactly as before), with a caveat line when some but not all sales in the window have a cost entered.

- **Best Offer accept / decline / counter UI, shipped Sept 9, 2026.** Sellers who enable Best Offer on a listing previously had to leave the app and use eBay's own site/app to respond to an offer -- there was no in-app way to see or act on one at all. Built against eBay's Trading API docs (BestOfferType, BestOfferStatusCodeType, RespondToBestOfferRequestType -- all fetched and verified field-by-field before writing any code, not assumed from memory): `src/lib/ebay-listings.ts` is a new shared module holding the XML-parsing + "fetch every page of My eBay Selling" helpers that used to live only in `src/app/api/ebay/store/route.ts` (`store/route.ts`'s own `GET()` behavior is byte-for-byte unchanged, just re-imported -- diffed to confirm). `GET /api/ebay/offers` reuses that shared `fetchAllOfListType("ActiveList")`, then calls `GetBestOffers` per active listing with the same bounded worker-pool pattern `sales/route.ts` already uses for gallery lookups (`OFFERS_LOOKUP_CONCURRENCY = 5`, `OFFERS_LOOKUP_MAX = 60`, `truncated` flag when a seller has more active listings than the cap), keeping only offers whose `Status` is `"Pending"` -- confirmed against eBay's own `BestOfferStatusCodeType` reference that *within a GetBestOffers response* `Pending` (not `Active`, which exists as an enum value but means something else there) is what "awaiting the seller's response" actually is; getting that wrong would have silently shown zero actionable offers. `POST /api/ebay/offers/respond` validates `{itemId, bestOfferId, action: "accept"|"decline"|"counter", counterPrice?}` (`action` maps to eBay's own enum -- note it's `Decline`, not `Deny`, another spot the docs could mislead a guess) and calls `RespondToBestOffer`. New `src/app/offers/page.tsx` (entry point: a "Pending offers" card on `/store`, not a 5th bottom-nav tab -- checking offers costs one eBay call per active listing, too expensive to fire on every nav) lists each pending offer with the buyer, offer amount and % of asking price, and an expiration countdown computed from eBay's own `ExpirationTime` rather than assuming a fixed 24h/48h window (eBay's docs aren't even internally consistent about which). Accept/Decline/Counter each require a `window.confirm(...)` first, same pattern as `/store`'s existing delist button, since all three are irreversible communications to a real buyer. **What this pass could verify and what it couldn't:** `tsc`/`eslint` clean, all 46 Vitest tests still passing, a dev-server smoke test confirming `/offers` and both new API routes don't 500 (401/307 as expected while signed out, matching every other auth-gated route). What it could NOT verify: whether clicking Accept/Decline/Counter on a *real* pending offer actually does the right thing on eBay's side -- there's no way to generate a real buyer offer to test against from this environment. Sel should try it against a real Best Offer once one comes in and flag anything that looks off.

- **Self-service password reset, shipped Sept 9, 2026.** `/login` had no way back in for a forgotten password -- the only recovery was asking Sel to reset it directly in the Supabase dashboard. Added `/forgot-password` (calls `supabase.auth.resetPasswordForEmail(email, {redirectTo: origin + "/reset-password"})`) and `/reset-password` (waits for the client SDK's own automatic `?code=`/`#access_token=` exchange -- confirmed against Supabase's docs that calling `exchangeCodeForSession` manually here would race the SDK's own automatic one over the same one-time-use code -- then calls `supabase.auth.updateUser({password})`), both added to `PUBLIC_PATHS` in `src/middleware.ts`. **Needs one manual dashboard step Sel still has to do before this works end-to-end:** Supabase Auth only honors a `redirectTo` that matches an allow-listed URL (Authentication -> URL Configuration -> Redirect URLs) -- add the app's real origin's `/reset-password` (or a `<domain>/**` wildcard) there, no tool here can read or set that config.
- **Pending-offers visibility on the Dashboard, shipped Sept 9, 2026.** The offers feature (shipped earlier the same day) only had an entry point on `/store` -- easy to miss since checking for offers isn't most sellers' first stop. Dashboard now fires `GET /api/ebay/offers` alongside its other independent per-visit fetches and shows a "Pending offers" card (same visual pattern as the existing "To ship" card, page-cached the same way) with a live count badge. Deliberately still not a bottom-nav badge -- see the "too expensive to fire on every nav" reasoning already in the Best Offer bullet below; a dashboard visit is a fundamentally different cost class than every in-app navigation.
- **Store page: fixed a dead completeness check, and shipped a real Active/Ended split with Relist, Sept 9, 2026.** Two related things found and fixed together while auditing the app end to end:
  1. `store/page.tsx` read `data.total` from `/api/ebay/store`'s response to decide whether the eBay listing set it got back was complete enough to trust for dropping presumed-sold Supabase-tracked items -- but that route has never returned a field called `total` (its real fields are `activeTotal`/`unsoldTotal`, added when it started returning ended/unsold listings too). `data.total` was always `undefined`, so the check (`total <= ebayListings.length`) was always true and the "fetch might be incomplete, don't drop anything" safety branch could never fire. Fixed to compare against the real `activeTotal + unsoldTotal`.
  2. Once that merge logic was being touched anyway, built the tabbed Active/Ended view CLAUDE.md had described since before this audit but which was never actually built: `status`/`endTime` now survive the merge (previously dropped), an Active/Ended segmented control replaces the old single flat list, and Ended rows get a real "Relist" action -- `RelistFixedPriceItem` (confirmed against eBay's own Trading API docs that fixed-price listings, all this app ever creates, need this call specifically, not the plain `RelistItem`; eBay assigns the relisted item a *new* ItemID by default, which the row swaps to) via new `POST /api/ebay/relist`, mirroring `delist/route.ts`'s shape exactly. Bulk price editing (Select mode) stays scoped to the Active tab only.
- **Sales CSV export, shipped Sept 9, 2026.** `/sales` had no way to get the numbers out of the app -- a seller doing their own books had to retype every row by hand. Added an export button (header, next to refresh) that builds a CSV client-side from whatever's already loaded (date, title, listing ID, qty, price, total, est. fee, cost, profit -- profit only populated when a cost was actually entered, never silently conflating "revenue minus estimated fee" with real profit) and downloads it via a `Blob` + temporary link, UTF-8 BOM included so Excel doesn't mis-detect encoding on titles with non-ASCII characters.
- **Real (not just estimated) eBay fee totals, shipped Sept 9, 2026 -- adds the `sell.finances` OAuth scope.** The fee/net-profit numbers on `/sales` have been an estimate since they shipped (see the bullet below) because this app's eBay OAuth connection was never granted the scope that unlocks eBay's real per-order fee ledger. Added `https://api.ebay.com/oauth/api_scope/sell.finances` to `EBAY_SCOPES` in `src/lib/ebay-oauth.ts` (scope URI verified against eBay's own OAuth scope list and its Finances API OpenAPI spec before adding it, not guessed -- an unrecognized scope string would break the whole eBay-connect consent screen, not just this feature) and a new `src/lib/ebay-finances.ts` that calls the Sell Finances API's `GET /transaction` (note: served from `apiz.ebay.com`, a *different host* than every other REST call in this app, which all hit `api.ebay.com` -- confirmed against the same spec) to sum real `totalFeeAmount` across every `SALE` transaction in the requested date window. Surfaced on `/sales` as a separate, clearly-labeled "Actual fees eBay reported for this period" line -- deliberately NOT merged into or swapped for the existing per-sale estimate, since `GetSellerTransactions` (Trading API, what builds the sale list itself) and the Finances API don't have a guaranteed one-to-one row correspondence (order grouping, timing/timezone edges), so claiming an exact per-row replacement would risk silently misattributing a real number. Falls back to nothing (not an error) when the scope isn't granted yet or the call fails for any reason. **Anyone already connected needs to reconnect once (Settings -> Reconnect) for this new scope to take effect** -- same story as every previous scope addition in this app (`sell.account.readonly`, Phase 2). **Flagged, not fully live-verified:** this was built and reasoned through against eBay's own published OpenAPI spec and OAuth scope list, the same rigor as the Best Offer work below, but there's no live connected account to test the actual OAuth consent screen or a real Finances API response against from this environment -- if reconnecting ever shows an "invalid scope" error, or the "actual fees" figure looks obviously wrong once real data comes back, that's the one thing to flag back immediately.
- **Three listing-workflow friction fixes, shipped Sept 10, 2026 -- found by walking the actual listing flow end to end looking for repetitive-tap friction, not a reported bug.**
  1. **"List another item" on `/new-listing` without losing the page.** Previously, a successful "List on eBay" always `setTimeout`'d a hard redirect to `/store` 1.5s later -- fine for one item, but a seller doing several single-item listings in a row had to fully navigate back to `/new-listing` and start from a blank form every single time (batch-upload doesn't have this problem, it's already built for many-at-once). The redirect is gone; a successful listing now shows an explicit two-button choice ("List another item" / "Done — go to Store") instead of deciding for the seller. "List another item" calls a new `resetForOtherItem()` that clears every per-item field (photos, title, AI result, price, cost, store category, the file-input DOM elements themselves so re-picking the same filename still fires `onChange`) but leaves account-level state (fetched store categories, the AI-store-category-suggestions setting) alone.
  2. **The "eBay wants these fields" warning was a dead end.** When `missingRequiredAspects` comes back non-empty (a category has a required item specific the AI couldn't determine), all three listing screens told the seller which fields were missing but gave no way to act on it -- they'd have to go find that exact listing on eBay's own site themselves. `/api/ebay/list` already returns a `url` field (the public `ebay.com/itm/<id>` link) computed from the same `listingId`; that field just wasn't being read on `new-listing`/`batch-upload`, and on `drafts/[id]` it was captured (`listingUrl`) but only ever used for the page's own "View on eBay" button, not linked from this specific warning. Wired an "Open the listing on eBay to add them →" link into the warning on all three screens using that existing `url`/`listingUrl` value -- no new eBay call, no guessed deep-link URL (eBay doesn't document one for jumping straight to a listing's Revise form, so this links to the real, confirmed-correct item page instead of a guessed edit-mode URL that could easily 404).
  3. **"Duplicate this item."** No way existed to spin off a near-identical listing (the same shirt in another size, more of one lot) without redoing the whole AI-analysis flow from scratch every time. New `POST /api/drafts/[id]/duplicate` copies every descriptive/eBay-facing field (title, brand, color, size, condition, flaws, all the item-specifics columns, store category, cost basis, heavy-shipping flag/cost, and the pricing-comp fields as a starting point) into a fresh draft row, but deliberately leaves `photo_urls`/`thumbnail_url` null (a duplicate is a different physical item -- carrying over the source's photos would misrepresent condition, which cuts against this app's whole point of showing buyers the actual item), and clears `custom_sku`/`ebay_listing_id` (a fresh SKU auto-assigns at listing time same as any new draft; the copy is never listed yet regardless of whether the source was). A duplicate (copy) icon button was added both per-row on `/drafts` (next to the row's existing chevron, `stopPropagation`'d so it doesn't also trigger the row's own navigate-in click handler) and in the header of `/drafts/[id]` (next to the existing delete button) -- both navigate straight into the new draft afterward so the seller can add real photos and adjust size/price right away.

  **Verify:** `tsc`/`eslint` clean (same 13 pre-existing `react-hooks/set-state-in-effect`-class warnings as before this change, no new ones), all 60 Vitest tests still passing, full diff re-read file by file. What this pass could NOT verify from this environment: an actual live eBay listing publish (no live connected account here) to confirm the `url`/`listingUrl` link genuinely opens the right item page end to end -- the field itself is the same one `/api/ebay/list` has already been returning and this app has already been using elsewhere (`drafts/[id]`'s own "View on eBay" button), just newly linked from one more place, so the risk here is low, but worth a glance the next time a listing publishes with a missing-aspects warning.

- **Returns/refunds tracking -- researched, deliberately NOT built Sept 9, 2026.** The natural next item on the same list as the fee/notifications/offers work above, but research surfaced enough real uncertainty that shipping a guess would have been worse than not shipping: eBay manages buyer return requests through the older Post-Order API (`/post-order/v2/return/search`, `/return/{id}/decide`, `/return/{id}/issue_refund`), which (a) uses a non-standard `Authorization: IAF <token>` header instead of the `Bearer <token>` scheme every other REST call in this app uses, and (b) has a required OAuth scope that even eBay's own developer community forum threads disagree about / couldn't confirm -- unlike `sell.finances` above, no authoritative single source (an OpenAPI spec, a clean scope-list table) could be found to verify it against. Given this deals with real buyer disputes and refund actions on Sel's actual eBay account, and there's no live account to test against from here either way, building it on an unconfirmed scope and an unconfirmed auth header would risk either breaking the eBay OAuth consent flow outright or silently shipping a broken/incomplete view of open returns. Left for a future session where either eBay's docs render more completely or it can be built and tested against a real connected account directly.

## Deferred

| Feature | Notes |
|---|---|
| **Daily email digest: no hourly trigger yet** | Found Sept 9, 2026 during deploy -- Vercel's free "Hobby" plan only allows daily cron jobs, not the hourly schedule the digest route needs to check each user's chosen send time (see the dated bullet above). The hourly entry was removed from `vercel.json` so deploys stop failing; `/api/cron/daily-digest` still exists and works correctly when called, it just has nothing triggering it hourly right now. Needs a free outside scheduler (e.g. cron-job.org) hitting it once an hour with the `CRON_SECRET` bearer header -- not yet set up. Until then, the Settings toggle exists but nothing sends even when turned on. |
| **In-app shipping label purchase** | Researched Sept 9, 2026 — eBay's Logistics API (the only API that actually buys a label) is explicitly a "Limited Release" API requiring eBay business-unit approval, not obtainable by just adding an OAuth scope, and there's no way to request/check that approval from this environment. Deferred fully; see `BACKLOG.md` for the plain-English version. |
| **Stripe billing** | Membership page is UI only — deferred until all features are working |
| ~~**Heavy shipping per-item cost**~~ | Done — `src/lib/shipping.ts` now estimates a real per-item weight and cost from item type/size/material; the dollar field is auto-filled but still user-editable |
| **Supabase leaked-password protection** | Re-checked Sept 9, 2026 (`get_advisors`) — still flagged disabled (`auth_leaked_password_protection`, WARN). Not settable via any available Supabase MCP tool (it's an Auth/GoTrue service config, not a DB row) — needs the Dashboard: Authentication → Sign In / Providers → Password → enable "Leaked password protection." One click, no code change. Also logged in `BACKLOG.md` in plain English (Sel's call: revisit once other people are using the app with their own passwords). |
| **Supabase Auth Redirect URLs for password reset** | New Sept 9, 2026, alongside the forgot-password/reset-password pages above. `resetPasswordForEmail`'s `redirectTo` only works if it matches an allow-listed URL in the Supabase Dashboard (Authentication → URL Configuration → Redirect URLs) — not settable via any available Supabase MCP tool. Add the app's real origin's `/reset-password` (or a `<domain>/**` wildcard). Until this is added, the reset email itself still sends, but the link back into the app will be rejected by Supabase. |
| **Returns/refunds tracking** | Researched Sept 9, 2026, deliberately not built — see the dated bullet above for why (unverifiable OAuth scope + non-standard `IAF` auth header on eBay's Post-Order API, no live account to test against). Worth revisiting with either better-rendering docs or a live connected account in hand. |
| ~~**Next.js major-version upgrade (15/16)**~~ | Done — the app is actually on Next.js 16.3.4 / React 19 already (confirmed via `package.json` and `git log` Sept 9, 2026; this row was stale, describing a pre-upgrade state that no longer matched the code). `images.unoptimized: true` in `next.config.js` remains correct/harmless regardless of version. New, found the same day: `next dev`'s own startup output now warns `"middleware" file convention is deprecated. Please use "proxy" instead` — `src/middleware.ts` still works fine on 16.3.4, this is a deprecation notice, not a break, but a future session migrating it to `proxy.ts` (`npx @next/codemod@canary middleware-to-proxy .`) would close it. Not attempted this round — out of scope for this pass and the app's central auth gate deserves its own careful, standalone verification, not a drive-by rename. |
| **`npm audit` dev-dependency findings** | Re-run Sept 9, 2026: 0 vulnerabilities in production dependencies (`npm audit --omit=dev`). All 5 flagged (3 moderate, 1 high, 1 critical) are inside the `vitest@2.1.9` → `vite`/`esbuild`/`vite-node` dev-only chain — the same deliberate pin documented in the Automated Tests bullet above (vitest 5 conflicts with this repo's pinned `@types/node`). None reachable in production; the "critical" one specifically needs Vitest's UI server, which this repo's `npm test` (`vitest run`) never starts. Left as-is, same reasoning as before, just re-verified rather than assumed. |
| ~~**Automated tests**~~ | Done -- Vitest added Sept 9, 2026 (see above), 46 tests covering the money/matching-logic libs, wired into CI. Component/e2e tests (Playwright etc.) remain a reasonable later addition, not attempted here. |
| ~~**COGS (cost-of-goods) tracking**~~ | Done -- shipped Sept 9, 2026 (see above), same day the gap was flagged. |
| **`batch-upload/page.tsx` refactor** | Flagged Sept 9, 2026 during the workflow review -- 2,499 lines, 11 `useState`s, ~34 handlers, the largest file in the app by a wide margin. Real maintenance cost, but splitting it is a meaningful, risky refactor with no direct user-facing benefit -- not attempted alongside this round's user-facing work. Technical debt only, not added to `BACKLOG.md` (nothing here needs Sel's input). |
| **SKU auto-assign race on bulk "List all"** | Flagged Sept 9, 2026. Already fails loudly via the DB's unique constraint on `custom_sku` rather than silently double-assigning -- just isn't auto-retried after a collision. Minor, already documented in the code's own comments. |
| **Scattered `react-hooks/set-state-in-effect` lint warnings** | Flagged Sept 9, 2026. Beyond the three already-known instances (`Toast.tsx`, `account/pending-deletion`, `drafts/[id]`, `settings`), a few more exist app-wide (e.g. `dashboard/page.tsx`, `store/page.tsx`, confirmed still present after this round's own edits). Pre-existing pattern, not a correctness bug -- a real cleanup pass is bigger than "while we're in the file" scope. |
